# Desktop binary assets

GitHub does not allow storing binary files in this repository. Create the following
files locally when building desktop installers:

- `xjson-desktop/src-tauri/icons/icon.icns`
- `xjson-desktop/src-tauri/icons/icon.ico`

Use the `.md` placeholder files in this folder for format/size reminders.
