# icon.icns (placeholder)

GitHub does not allow storing binary files in this repository. Create a macOS
`.icns` file and place it at `xjson-desktop/src-tauri/icons/icon.icns` before
packaging the desktop app.
